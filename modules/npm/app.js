// Rules apply when we don't specify a relative path
// Rule 1: core module of that name
// Rule 2: local node_modules directory
// Rule 3: the parent directory for node_modules
//
var requests = require('requests')
require('express')
